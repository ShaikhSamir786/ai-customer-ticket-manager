{
  "name": "@ai-ticket/shared-schema",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "echo 'lint ok'",
    "typecheck": "tsc --noEmit",
    "clean": "node -e \"require('fs').rmSync('dist', { recursive: true, force: true })\"",
    "db:migrate": "ts-node scripts/migrate.ts",
    "migrate:undo": "ts-node scripts/migrate-undo.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.0",
    "pg": "^8.21.0",
    "reflect-metadata": "^0.2.2",
    "sequelize": "^6.37.8",
    "sequelize-typescript": "^2.1.6"
  },
  "devDependencies": {
    "ts-node": "^10.9.0",
    "typescript": "^5.6.0"
  }
}
